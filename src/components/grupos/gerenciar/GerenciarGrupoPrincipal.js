import React from 'react';
import { View } from 'react-native';

import { connect } from 'react-redux';

class GerenciarGrupoPrincipal extends React.Component {
    render = () => {
        return (<View />);
    }
}

export default connect()(GerenciarGrupoPrincipal);
